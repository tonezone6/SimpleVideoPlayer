import XCTest
@testable import SimpleVideoPlayer

final class SimpleVideoPlayerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SimpleVideoPlayer().text, "Hello, World!")
    }
}
